'use client';

import React from 'react'

const AddToCard = () => {
  return (
    <div>
      <button onClick={() => console.log("Click")}>Add this product to Card</button>
    </div>
  )
}

export default AddToCard
